export default function eLoading(text = '', color = '#409EFF', textColor = '#fff', maskColor = 'transparent') {
  return {
    text: text,
    color: color,
    textColor: textColor,
    maskColor: maskColor,
    effect: 'whirling'
  }
}

// maskColor rgba(0, 0, 0, 0.4)
